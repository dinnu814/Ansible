---
-  hosts: webservers
   become: yes
   tasks:
   - name: Install Apache server
     yum:
        name: httpd
        state: present
   - name: ensure apache service is running
     service:
        name: httpd
        state: started
        enabled: yes
